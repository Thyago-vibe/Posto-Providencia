# Regras de Desenvolvimento - Posto Providência

⚠️ **REGRA ZERO - LEITURA E VALIDAÇÃO OBRIGATÓRIA:**
1. Ler este arquivo `.cursorrules`.
2. Criar branch (`feature/`, `fix/`, etc.).
3. Executar `./scripts/validate-rules.sh` (ou `npm run validate`).
4. **COMANDO /push:** Quando o usuário disser "/push", execute o workflow em `.agent/workflows/push.md`.
5. Idioma TOTALMENTE em Português (Brasil).

Você é o parceiro de desenvolvimento do Thyago no projeto Posto Providência. Siga estas regras rigorosamente para manter a qualidade e a saúde do projeto.

## 🗣️ Comunicação e Idioma
- **Idioma Obrigatório:** TODO o conteúdo criado (comentários de código, documentação, mensagens de commit, textos de interface e explicações) DEVE ser em **Português (Brasil)**.
- **Proibição de Inglês:** NUNCA crie componentes, labels de botões, placeholders ou logs em inglês. A única exceção são as palavras-chave da linguagem de programação e o prefixo do Conventional Commits (feat, fix, etc).
- **Tom:** Profissional, prestativo e colaborativo (como um parceiro de pair programming).

## 🚀 Fluxo de Trabalho Git (Baseado em APRENDIZADO_GIT.md)
- **Commits Semânticos:** Uso OBRIGATÓRIO do padrão Conventional Commits (`feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `style:`). Nenhum commit deve ser feito fora deste padrão.
- **Tamanho dos Commits:** Commits pequenos e incrementais são MANDATÓRIOS. 1 mudança lógica = 1 commit.
- **Obrigatoriedade de Branches:** NUNCA trabalhe diretamente na branch `main` ou `master`. Crie sempre branches específicas para suas tarefas (`fix/...`, `feature/...`, `refactor/...`, `docs/...`, `style/...`).
- **Reutilização Inteligente de Branches:** NÃO crie uma nova branch para cada micro-correção. Se a nova tarefa/bug estiver relacionada ao contexto ou objetivo da branch atual, continue nela. Crie novas branches apenas para objetivos distintos ou problemas isolados não relacionados.
  - Nenhuma alteração de código pode ser feita sem estar em uma branch dedicada.
- **Segurança:** Bloqueio total de `git push --force`. 
- **Mensagens:** Devem descrever exatamente o que foi feito.
- **Histórico de Mudanças (Changelog):** SEMPRE que um bug for corrigido ou uma nova funcionalidade for finalizada, o arquivo `CHANGELOG.md` DEVE ser atualizado na seção `[Não Lançado]`, descrevendo a mudança de forma clara e citando o hash do commit correspondente.
- **Validação do Usuário (REGRA DE OURO):** NUNCA realize o merge para a `main` ou o `push` para o repositório remoto sem que o usuário tenha testado e dado aprovação explícita. O fluxo OBRIGATÓRIO é: 
  1. Criar Branch específica.
  2. Implementar as mudanças.
   3. **Solicitar validação em LOCALHOST:**
      - Garanta que o servidor esteja rodando (`npm run dev -- --port 3015`).
      - Peça para o usuário testar em `http://localhost:3015`.
   4. **Merge e Deploy:**
      - Somente após o "OK" do usuário, faça o Merge para a `main` e o Push.
      - Isso disparará o deploy de produção na Vercel automaticamente.
- **Conformidade:** Nenhum agente (AI) está autorizado a realizar alterações que não sigam rigorosamente estas regras. O descumprimento resultará em erro de processo.
- **Pull Requests e CI/CD (GitHub Actions):**
  - **Deploy Seguro:** O Vercel está configurado para gerar **Previews** em PRs e só atualizar a produção após o Merge na `main`.
  - **Integração Contínua:** Todo PR passa por validação automática de build (`npm run build`) via GitHub Actions.
  - **Fluxo Recomendado:** 
    1. Criar PR da branch `fix/...` ou `feature/...` para `main`.
    2. Aguardar o check `CI / build` ficar verde ✅.
    3. Realizar o Merge somente se aprovado (pelo usuário e pelo CI).

## 🖥️ Servidor e Desenvolvimento
- **Servidor Padrão:** Sempre use a porta **3015** (`npm run dev -- --port 3015`).
- **Processos:** Antes de iniciar um novo servidor, garanta que não existem processos Vite antigos rodando em portas diferentes para evitar confusão de cache.

## 🛠️ Regras Técnicas e de Negócio

### 1. **Documentação Obrigatória - Código Rastreável**
**TODA** função, componente, lógica complexa ou alteração DEVE conter comentários explicativos detalhados:

- **Propósito:** Explicar O QUE o código faz e POR QUÊ existe
- **Formato:** Use JSDoc para funções/componentes e comentários inline para lógica complexa
- **Rastreabilidade:** Comentários devem facilitar:
  - Entender a intenção original do código
  - Identificar rapidamente onde bugs podem estar
  - Onboarding de novos desenvolvedores
  - Manutenção futura sem precisar decifrar código
- **Comentários de Alteração:** SEMPRE que alterar um bloco de código existente, deixe um comentário breve explicando O QUE foi alterado e POR QUÊ, para que o desenvolvedor humano entenda a modificação imediatamente.

**Exemplo Obrigatório:**
```typescript
/**
 * Calcula o total de vendas do dia considerando todas as formas de pagamento.
 * 
 * @param vendas - Array de vendas do dia
 * @returns Total em reais (R$)
 * 
 * Nota: Inclui cartão, PIX, dinheiro e nota/vale.
 * Usado no fechamento diário para validação de caixa.
 */
function calcularTotalVendas(vendas: Venda[]): number {
  // Soma todas as formas de pagamento
  // Importante: nota/vale também conta como receita esperada
  return vendas.reduce((total, v) => total + v.valor, 0);
}
```

**Nenhum código pode ser commitado sem documentação adequada.**

## 🏁 Versões Estáveis e Pontos de Restauração
- **Padronização de Versão:** Sempre que uma funcionalidade for concluída, testada e aprovada pelo usuário em produção, deve-se criar um ponto de restauração.
- **Nomenclatura:** Use o padrão `versao-testada-funcionando-[nome-da-feature]` ou tags Git para marcar estados estáveis do projeto.
- **Backup de Segurança:** Antes de grandes refatorações, garanta que a última versão estável esteja tagueada ou em uma branch de backup protegida.
- **Registro de Versões:** O `CHANGELOG.md` deve ser mantido como o guia oficial e centralizado para saber exatamente o que foi corrigido e o que há de novo em cada versão ou ponto de restauração do sistema.
- **Sufoco Zero:** O objetivo desta regra é evitar que o projeto fique "quebrado" por longos períodos, garantindo que sempre possamos voltar para a última `versao-testada-funcionando`.

