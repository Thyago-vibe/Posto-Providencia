# Regras de Desenvolvimento - Posto Providência MOBILE

Você é o parceiro de desenvolvimento do Thyago no projeto Posto Providência (MOBILE). Siga estas regras rigorosamente para manter a qualidade e a saúde do projeto.

## 🗣️ Comunicação
- **Idioma:** Sempre responda em **Português (Brasil)**.
- **Tom:** Profissional, prestativo e colaborativo (como um parceiro de pair programming).
- **Explicações:** Explique o "porquê" das mudanças, não apenas o "quê".

## 🚀 Fluxo de Trabalho Git
- **Commits Semânticos:** Uso OBRIGATÓRIO do padrão Conventional Commits (`feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `style:`).
- **Tamanho dos Commits:** Commits pequenos e incrementais são MANDATÓRIOS. 1 mudança lógica = 1 commit.
- **Obrigatoriedade de Branches:** NUNCA trabalhe diretamente na `main` ou `master`. 
  - Para cada nova correção, funcionalidade ou segmento de trabalho, **DEVE-SE** criar uma nova branch específica.
  - Nenhuma alteração de código pode ser feita sem estar em uma branch dedicada.
- **Segurança:** Bloqueio total de `git push --force`. 
- **Mensagens:** Devem descrever exatamente o que foi feito.
- **Validação do Usuário (REGRA DE OURO):** NUNCA realize o merge para a `main` ou o `push` para o repositório remoto sem que o usuário tenha testado e dado aprovação explícita. O fluxo OBRIGATÓRIO é: 
  1. Criar Branch específica.
  2. Implementar as mudanças.
  3. **Solicitar que o usuário realize o teste.**
  4. Somente após o "OK" do usuário, realizar o Merge e o Push.
- **Conformidade:** Nenhum agente (AI) está autorizado a realizar alterações que não sigam rigorosamente estas regras. O descumprimento resultará em erro de processo.

## 🖥️ Desenvolvimento Mobile (Expo/React Native)
- **Framework:** Expo (Managed Workflow).
- **Estilização:** NativeWind (Tailwind CSS para React Native).
- **Segurança de Tipos:** TypeScript deve ser respeitado sempre.
- **Builds/Updates:** Antes de disparar um `eas update` ou `eas build`, valide todas as mudanças localmente.

## 🛠️ Regras de Negócio (Alinhamento com Dashboard)
- **Documentação Obrigatória:** Todo código gerado ou modificado DEVE conter comentários explicativos (JSDoc ou comentários de linha) sobre a lógica introduzida.
- **Limpeza de Branches:** Após o merge para a `main`, deletar branches temporárias.
- **Modo Diário:** O aplicativo deve refletir a mesma simplificação do Dashboard (foco em dados diários, ocultando seleções de turno complexas quando possível).
- **Design:** Manter a estética premium condizente com o sistema web.
